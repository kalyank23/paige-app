import { TypeToTitlePipe } from './type-to-title.pipe';

describe('TypeToTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeToTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
